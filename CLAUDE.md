# drift

<!-- vault-link:start -->
## Vault link

The project note for this repo lives in the Obsidian vault at `/Users/mcint/notes`:

    /Users/mcint/notes/01-Projects/Drift.md

When work here changes the project's state, update that note — specifically the
`next_action` frontmatter property and the `## Log` section. Do not create a
duplicate tracking file inside this repo.

The vault is a separate tree. Do not index it from here, and do not add it to
this repo's search paths.
<!-- vault-link:end -->
